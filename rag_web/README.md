# RAG Web Application

Веб-приложение для RAG системы поиска геологических данных Каспийского моря.

## Структура проекта

```
rag_web/
├── backend/          # Django backend
│   ├── config/      # Настройки Django
│   ├── rag_api/     # API endpoints
│   └── manage.py
└── frontend/        # React frontend
    ├── src/
    │   ├── components/
    │   │   ├── QueryForm.js      # Форма запроса
    │   │   ├── ResultsDisplay.js  # Отображение ответа
    │   │   ├── MapDisplay.js      # Яндекс карта
    │   │   └── VideoAvatar.js     # Заглушка для HeyGen
    │   ├── App.js
    │   └── index.js
    └── public/
```

## Возможности

- ✅ Одностраничное веб-приложение
- ✅ Поле для ввода запроса с кнопкой отправки
- ✅ Прогресс-бар во время обработки запроса
- ✅ Отображение ответа преподавателя
- ✅ Яндекс карта с метками найденных координат
- ✅ Автоматическое определение области видимости для множественных координат
- ✅ Поле для видео-аватара HeyGen (заглушка)

## Установка и запуск

### Быстрый запуск

Используйте готовые скрипты:

```bash
# Backend (в отдельном терминале)
./start_backend.sh

# Frontend (в отдельном терминале)
./start_frontend.sh
```

### Ручная установка

#### Backend (Django)

1. Перейдите в директорию backend:
```bash
cd rag_web/backend
```

2. Создайте виртуальное окружение (если еще не создано):
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Примените миграции:
```bash
python manage.py migrate
```

5. Запустите сервер:
```bash
python manage.py runserver
```

Backend будет доступен на http://localhost:8000

#### Frontend (React)

1. Перейдите в директорию frontend:
```bash
cd rag_web/frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev сервер:
```bash
npm start
```

Frontend будет доступен на http://localhost:3000

## Настройка Яндекс карт

1. Получите API ключ на https://developer.tech.yandex.ru/
2. Замените `YOUR_YANDEX_MAP_API_KEY` в `frontend/public/index.html` на ваш ключ

Пример:
```html
<script src="https://api-maps.yandex.ru/2.1/?apikey=ваш_ключ_здесь&lang=ru_RU" type="text/javascript"></script>
```

## API Endpoints

### POST /api/query/

Обработка запроса пользователя.

**Request:**
```json
{
  "query": "Где R0 больше 1.0%?"
}
```

**Response:**
```json
{
  "answer": "Ответ преподавателя...",
  "coordinates": [
    {
      "lon": 50.123,
      "lat": 40.456,
      "info": "layer_name: Слой 1, Регион: Южный Каспий"
    }
  ],
  "results_count": 10
}
```

## Особенности реализации

### Извлечение координат

Функция `extract_coordinates()` в классе `RAGSystemLangChain`:
- Извлекает координаты из DataFrame результатов
- Обрабатывает координаты в формате массивов `[lon1, lon2, ...]`
- Валидирует координаты (проверка диапазонов)
- Собирает дополнительную информацию о каждой точке

### Яндекс карта

Компонент `MapDisplay`:
- Автоматически создает метки для всех найденных координат
- При множественных координатах автоматически устанавливает границы видимости
- Показывает подсказки и балуны с информацией о точке

### Прогресс-бар

Прогресс-бар показывает приблизительный прогресс обработки запроса:
- 0-90%: симуляция прогресса во время обработки
- 100%: завершение обработки

## Технологии

- **Backend**: Django 4.2+, Python 3.8+
- **Frontend**: React 18.2+, JavaScript ES6+
- **Карты**: Яндекс.Карты API
- **Стили**: CSS3 с градиентами и анимациями

## Деплой проекта

### Вариант 1: Beget для сайта + VPS для OpenSearch

Для деплоя на хостинг Beget см.:
- `QUICK_DEPLOY.md` - краткая инструкция
- `DEPLOY_BEGET.md` - подробная инструкция с решением проблем
- `OPENSEARCH_DEPLOY.md` - ⚠️ **ВАЖНО:** Инструкция по развертыванию OpenSearch на VPS

### Вариант 2: Все на VPS сервере

Если у вас есть VPS и вы хотите разместить весь проект (сайт + OpenSearch) на одном сервере:
- `VPS_HOST_SETUP.md` - 🆕 Полная инструкция по размещению проекта на VPS

## Примечания

- ⚠️ **OpenSearch:** Для локальной разработки убедитесь, что OpenSearch запущен на localhost:9200. Для продакшена на Beget нужно развернуть OpenSearch отдельно (см. `OPENSEARCH_DEPLOY.md`)
- Убедитесь, что у вас есть учетные данные GigaChat
- Для продакшена используйте переменные окружения через `.env` файл (см. `backend/env.example`)
- Для продакшена измените `SECRET_KEY` в `.env` и отключите DEBUG

